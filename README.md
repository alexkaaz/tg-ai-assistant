# tg-ai-assistant
text and image generative ai assistant 
AI assistant Telegram bot has two functions:

Text Manager - functions as a regular AI assistant. It can conduct a dialogue, answer questions, and perform various text-based tasks. Activated by using the command \text.

Image Generator - generates images based on user requests. After using the command \image_generator, it switches to generation mode. Only a prompt is required as input.
